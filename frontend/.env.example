# Local development defaults to http://127.0.0.1:8000 when this is empty.
# Production should point at the hosted FastAPI backend, for example:
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
